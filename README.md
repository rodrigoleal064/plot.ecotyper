# plot.ecotyper <img src="https://raw.githubusercontent.com/r-lib/usethis/main/man/figures/logo.png" align="right" height="139" alt="" />

[![Lifecycle: experimental](https://img.shields.io/badge/lifecycle-experimental-orange.svg)](https://lifecycle.r-lib.org/articles/stages.html#experimental)
[![R build status](https://github.com/rodrigoleal064/plot.ecotyper/workflows/R-CMD-check/badge.svg)](https://github.com/rodrigoleal064/plot.ecotyper/actions)
The **plot.ecotyper** package provides a robust and streamlined R interface for visualizing cell state abundance results generated by [EcoTyper](https://ecotyper.stanford.edu/). 

It automatically parses the complex EcoTyper output directory structure to generate publication-ready bar plots. The package supports stacked bar plots (absolute abundance), filled bar plots (relative abundance), dynamic sample annotation, and custom faceting.

## 🚀 Key Features

* **Smart Parsing:** Automatically reads valid cell type directories (e.g., `B.cells`, `CD8.T.cells`) while safely ignoring metadata folders like `Ecotypes`.
* **Dual Plot Modes:** Generate a single combined relative abundance plot or individual absolute abundance plots for each cell type.
* **Dynamic Grouping:** Map substrings in your sample names to clean, presentable groups for automatic `ggplot2` faceting.
* **Targeted Analysis:** Filter and visualize only specific cell types of interest.
* **Automated I/O:** Automatically creates output directories if they do not exist.

---

## 📦 Installation

You can install the development version of `plot.ecotyper` from GitHub with:

```r
# install.packages("devtools")
devtools::install_github("rodrigoleal064/plot.ecotyper")
```

---

## 💻 Usage & Examples

Load the package to get started:

```r
library(plot.ecotyper)
```

### 1. Combined Relative Abundance Plot
Generates a single, 100% stacked bar plot showing the proportion of all available cell types across your samples.

```r
plot_abundance(
  folder_path = "path/to/ecotyper/Recovery",
  plot_type = "combined",
  output_dir = "plots/overview"
)
```

### 2. Filtering Specific Cell Types
If you are only interested in a specific subset of the microenvironment (e.g., specific lymphoid and myeloid populations), you can explicitly request them using the exact folder names.

```r
plot_abundance(
  folder_path = "path/to/ecotyper/Recovery",
  plot_type = "combined",
  cell_types = c("B.cells", "CD8.T.cells", "Monocytes.and.Macrophages", "NK.cells"),
  output_dir = "plots/filtered_lymphoid"
)
```

### 3. Dynamic Sample Annotation and Faceting
Often, sample IDs contain metadata (e.g., `Patient1_AMR`, `Patient2_EUR`). You can provide a named vector to map these substrings to clean group names. The function will automatically group and facet the plots accordingly.

```r
# Define your dictionary: c("Substring_to_match" = "Clean Label")
my_metadata <- c(
  "AMR" = "American",
  "EUR" = "European",
  "ASN" = "Asian"
)

plot_abundance(
  folder_path = "path/to/ecotyper/Recovery",
  plot_type = "combined",
  sample_mapping = my_metadata,
  output_dir = "plots/faceted_populations"
)
```

### 4. Absolute Abundance per Cell Type
To inspect the raw abundance (stacking) of states within each specific cell lineage, generate individual plots. The package will save one PNG file per cell type (e.g., `Cell_type_abundance_Fibroblasts.png`).

```r
plot_abundance(
  folder_path = "path/to/ecotyper/Recovery",
  plot_type = "per_cell_type",
  sample_mapping = my_metadata,  # Faceting works here too!
  output_dir = "plots/individual_lineages"
)
```

---

## 🛠 Function Reference

### `plot_abundance()`

| Argument | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `folder_path` | `character` | `"files"` | Path to the root EcoTyper output directory containing the cell type subfolders. |
| `plot_type` | `character` | `"combined"` | `"combined"` (relative abundance of all cells) or `"per_cell_type"` (absolute abundance per cell). |
| `filter_assigned` | `logical` | `TRUE` | If `TRUE`, subsets the data to include only samples present in the `state_assignment.txt` file. |
| `sample_mapping`| `named vector`| `NULL` | Maps substrings in sample names to group labels for faceting. E.g., `c("AMR" = "American")`. |
| `cell_types` | `character` | `NULL` | Specific cell types to plot (must match folder names exactly). If `NULL`, plots all valid folders. |
| `output_dir` | `character` | `"."` | Path where PNGs will be saved. Created automatically if it does not exist. |

---

## 📝 Dependencies

This package relies on standard tidyverse tools to ensure fast and reliable data wrangling and visualization:
* `ggplot2`
* `dplyr`
* `magrittr`
* `rlang`
